import styled from 'styled-components';

const HeaderInputWrapper = styled.div`
  padding: 0 20px;
  flex-basis: 80%;
  display: flex;
  align-items: center;
  position: relative;
`;

export default HeaderInputWrapper;
