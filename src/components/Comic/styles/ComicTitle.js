import styled from 'styled-components';

const ComicTitle = styled.div`
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 5px;
  position: relative;
  padding-right: 30px;
`;

export default ComicTitle;
